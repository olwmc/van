println("Let\'s play a game!");

local start = 0;
local stop = 100;
local step = 7;

println("Count from " + start + " to " + stop + " in increments of " + step);

for(local i = start; i <= stop; i+= step) do
    println(i);
end