defun fib(n) as
  local a = 0;
  local b = 1;

  -- Loops through getting fibonacci numbers
  for(local i = 1; i < n; n = n + 1) do
    a = b;
    b = a + b;
  end

  return a;
end

assert(fib(8) == 21);